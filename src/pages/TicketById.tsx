import TicketByIdContainer from "~/components/tickets/TicketByIdContainer";
import PageWrapper from "~/containers/PageWrapper";

const TicketByIdPage: React.FC = () => {
  return (
    <PageWrapper>
      <TicketByIdContainer />
    </PageWrapper>
  );
};

export default TicketByIdPage;
